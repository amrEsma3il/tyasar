import 'package:flutter/material.dart';

class ReservationTime extends StatelessWidget {
  const ReservationTime({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("rrrrrrrrreservation_time"),
    );
  }
}